name: full_stack_project_oc
services:
  dsn_db:
    container_name: dsn_db
    image: mysql:8.4.3
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: network
      MYSQL_USER: ${MYSQL_USERNAME}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    restart: unless-stopped
    env_file:
      - path: .env
    networks:
      - spring-demo
  dsn_api:
    container_name: dsn_api
    build:
      context: ./back
      dockerfile: ../docker/backend/Dockerfile
    image: dsn/dsn_api:1.0.1
    ports:
      - 8088:8088
    networks:
      - spring-demo
    depends_on:
      - dsn_db

  dsn_ui:
    container_name: dsn_ui 
    build:
      context: ./front
      dockerfile: ../docker/frontend/Dockerfile
    image: dsn/dsn_ui:1.0.1
    ports:
      - 8080:80
    depends_on:
      - dsn_api
    networks:
      - spring-demo 


networks:
  spring-demo:
    driver: bridge
